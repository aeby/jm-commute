{
  "name": "jobmate-commute",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "public-transport:prepare": "node --import tsx scripts/public_transport/prepare.ts",
    "public-transport:matrix": "node --import tsx scripts/public_transport/matrix.ts",
    "road:prepare": "node --import tsx scripts/road/prepare.ts",
    "road:matrix": "node --import tsx scripts/road/matrix.ts",
    "commute:package:data": "node --import tsx scripts/commute/publish-package-data.ts",
    "commute:package:build": "node --import tsx scripts/commute/build-commute-package.ts",
    "commute:package:pack": "node --import tsx scripts/commute/pack-commute-package.ts",
    "commute:package:verify": "node --import tsx scripts/commute/verify-commute-package.ts",
    "commute:package:publish": "npm publish --workspace packages/commute --cache .npm-cache/publish",
    "commute:api:dev": "npm run dev --workspace @jobmate/commute-api",
    "commute:api:build": "npm run build --workspace @jobmate/commute && npm run build --workspace @jobmate/commute-api",
    "commute:api:start": "npm run commute:package:build && npm run build --workspace @jobmate/commute-api && npm run start --workspace @jobmate/commute-api",
    "commute:api:typecheck": "npm run build --workspace @jobmate/commute && npm run typecheck --workspace @jobmate/commute-api",
    "viewer:dev": "npm run dev --workspace @jobmate/commute-viewer",
    "viewer:build": "npm run build --workspace @jobmate/commute-viewer",
    "lint": "oxlint -D correctness -D suspicious --vitest-plugin --vue-plugin --deny-warnings src scripts packages/commute/src apps/commute-api/src apps/commute-viewer/src apps/commute-viewer/vite.config.ts vitest.config.ts",
    "test": "vitest run",
    "test:core": "vitest run --exclude 'apps/commute-viewer/**'",
    "typecheck:runtime": "tsc --noEmit -p packages/commute/tsconfig.json",
    "typecheck:core": "npm run typecheck:runtime && tsc --noEmit -p tsconfig.core.json && npm run commute:api:typecheck",
    "typecheck:viewer": "tsc --noEmit -p apps/commute-viewer/tsconfig.json",
    "typecheck": "npm run typecheck:core && npm run typecheck:viewer"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "oxlint": "^1.80.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "csv-parse": "^7.0.2"
  }
}
