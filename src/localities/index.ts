export { createLocalityId } from './locality-id';
export { createReachableLocalityMap } from './create-reachable-locality-map';
export { LocalityResolver } from './locality-resolver';
export { normalizeCityName } from './normalize-city-name';

export type {
  Locality,
  LocalityId,
  LocalityQuery,
  ReachableLocality,
} from './types';
